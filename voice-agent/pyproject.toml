[project]
name = "voice-agent"
version = "0.2.0"
description = "LiveKit voice AI agent with OpenAI (pipeline + realtime modes)"
requires-python = ">=3.13"
dependencies = [
    "livekit-agents[openai,silero]>=1.4,<1.5",
    "python-dotenv",
]
